{
  "name": "poornas-kitchen-android",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare-web": "node configure.js",
    "build:apk": "node configure.js && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2"
  }
}
